# Implementation mode: 'webhook' or 'temporal'
IMPLEMENTATION_MODE=webhook

# Webhook configuration (used when IMPLEMENTATION_MODE=webhook)
WEBHOOK_URL=https://your-n8n-webhook-url.com/webhook/search

# Temporal configuration (used when IMPLEMENTATION_MODE=temporal)
TEMPORAL_CLOUD_API_KEY=# If using temporal cloud
TEMPORAL_ADDRESS=
TEMPORAL_NAMESPACE=
TEMPORAL_TASK_QUEUE=enterprise-ai-agent

# Optional Temporal Cloud configuration
# TEMPORAL_TLS_CERT_PATH=/path/to/client.pem
# TEMPORAL_TLS_KEY_PATH=/path/to/client.key
# TEMPORAL_TLS_CA_CERT_PATH=/path/to/ca.pem
